<div class="sheret">
<div id='container'>
	<div class='create_post'>
	<%= form_for @post do |f| %>
		<div>
			<div> 
				<%= f.label :title, "Title"%>
				<%= f.text_field :title %>
				<%= f.label :content %>
				<%= f.text_field :content %>
			</div>

			<%= f.submit %>
		</div>
	<% end %>
	</div>
</div>
